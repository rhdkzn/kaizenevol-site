---
score: 70
p0: 0
p1: 4
p2: 3
p3: 3
timestamp: 2026-06-20T01-51-03Z
slug: index-html
---
---
score: 70
p0: 0
p1: 4
p2: 3
p3: 3
target: /home/user/kaizenevol-site/index.html
date: 2026-06-20
---

# Critique — KaizenEvol index.html

**Score: 70 / 100** — Good, minor issues remaining

This critique was run after five sequential improvement passes (animate → typeset → layout → polish → adapt). The page has no P0 blockers. All banned patterns from the initial state have been resolved except the ai-grid card format.

---

## Detector (Assessment B)

| Antipattern | Severity | Detail |
|---|---|---|
| `single-font` | warning | Only Poppins used for all text — no typographic contrast |
| `em-dash-overuse` | warning | 21 em-dashes in body copy (max ~2 before it reads as AI cadence) |
| `aphoristic-cadence` | warning | 5 aphoristic constructions ("X. Just Y." / "Not X. No Y." pattern) |

---

## Nielsen Heuristics

| # | Heuristic | Score (0–4) | Notes |
|---|---|---|---|
| 1 | Visibility of system status | 3 | Step stagger, active nav links, mobile menu transitions all work. No nav shadow/backdrop on scroll. |
| 2 | Match system and real world | 3 | Industry language solid. Em-dash cadence gives copy an AI texture. |
| 3 | User control and freedom | 3 | Mobile overlay closes on tap-outside. No ESC key handler. |
| 4 | Consistency and standards | 2 | Dead CSS (~35 lines of `.about-*` rules). `.mobile-link` class in HTML, no matching CSS rule. |
| 5 | Error prevention | 3 | `rel=noopener`, img fallbacks, `prefers-reduced-motion` all in place. |
| 6 | Recognition rather than recall | 3 | CTA repeated 4+ times. Fixed nav persistent. Clear hierarchy. |
| 7 | Flexibility and efficiency | 2 | No `<main>` landmark. No skip-to-content link. No ESC key on mobile drawer. |
| 8 | Aesthetic and minimalist design | 2 | ai-grid: 6 identical `{h3, p}` cards in 3×2 grid — still the banned pattern. Dead CSS adds file weight. Two consecutive `section-tint` sections (#pricing + #exclusivity) blur visual rhythm. |
| 9 | Help recognise/recover from errors | 3 | No forms, limited scope. External links well handled. |
| 10 | Help and documentation | 3 | Pricing explicitly redirects to call. FAQ linked in nav. |

**Average: 27 / 40 → 70 / 100**

---

## Issues

### P1 — Fix Before Going Live

| ID | Heuristic | Issue | Fix |
|---|---|---|---|
| I1 | H8 | **ai-grid: 6 identical `{h3, p}` cards in 3×2 grid** — banned identical card grid pattern persists after icon removal | Redesign rhythm: featured row + compact strip, or convert 2 cards to a numbered list/table format matching the problem section |
| I2 | H5 | **Fixed `.nav-wrap` uses `top: 16px`** — doesn't account for `env(safe-area-inset-top)` on notched iPhones; navbar overlaps status bar | `top: max(16px, env(safe-area-inset-top))` on `.nav-wrap` |
| I3 | H4 | **~35 lines of dead CSS**: `.about-inner`, `.about-stats`, `.stat-block`, `.stat-number`, `.stat-label`, `.about-bio` — no matching HTML in index.html | Delete. If About section moves to `about.html`, move styles there |
| I4 | H7 | **No `<main>` semantic landmark and no skip-to-content link** — screen reader users can't bypass marquee + nav to reach content | Wrap sections in `<main id="main-content">`, add visually-hidden skip link before marquee |

### P2 — Fix Before Launch

| ID | Heuristic | Issue | Fix |
|---|---|---|---|
| I5 | H2 | **21 em-dashes in copy** — detector flagged; AI cadence tell | Editorial pass: replace with commas, colons, or full stops |
| I6 | H8 | **Single font (Poppins)** — no typographic contrast between display headings and body | Add display weight variant or pair Poppins body with a contrasting display face for h1/h2 |
| I7 | H8 | **Two consecutive `section-tint` sections** (#pricing + #exclusivity) — visually merge | Change `#pricing` to `section-white` |

### P3 — Nice to Have

| ID | Heuristic | Issue | Fix |
|---|---|---|---|
| I8 | H3 | No ESC key handler for mobile menu | `document.addEventListener('keydown', e => { if (e.key === 'Escape') closeMenu(); })` |
| I9 | H4 | `.mobile-link` class on mobile anchors has no CSS rule (styles come from `.mobile-panel a`) | Remove class from HTML or add explicit rule |
| I10 | H2 | 5 aphoristic constructions in copy | Copyedit: vary sentence structure, favour specificity over rhythm |

---

## What's Working

- Motion system clean: hero entrance, step-card stagger with `--i` CSS variable, `prefers-reduced-motion` respected
- Banned patterns resolved: no numbered scaffolding, no eyebrow-on-every-section, problem section redesigned to editorial list
- Token system consistent: no hardcoded hex values outside `:root`, all gradients use tokens
- WCAG contrast fixed: `rgba(255,255,255,0.75)` on dark gradient ≈ 4.6:1
- Touch targets: hamburger 44×44, mobile close 44×44
- Safe area: footer bottom and container laterals correct
- `viewport-fit=cover` in meta tag
- Focus states on all interactive elements

---

## Recommended Next Steps

1. **I3 first** (dead CSS) — 2-minute fix, reduces file size, no risk
2. **I2** (nav safe area) — 1-line fix, prevents notch overlap on real devices
3. **I4** (`<main>` + skip link) — 15-minute fix, genuine a11y win
4. **I7** (consecutive tints) — 1-line class change
5. Then **I1** (ai-grid redesign) when copy direction is clear
